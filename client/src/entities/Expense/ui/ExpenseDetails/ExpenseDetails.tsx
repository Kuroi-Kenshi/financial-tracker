import { type FC } from 'react';
import s from './ExpenseDetails.module.scss';

interface ExpenseDetailsProps {
  className?: string;
}

export const ExpenseDetails: FC<ExpenseDetailsProps> = ({ className }) => {
  return <div></div>;
};
