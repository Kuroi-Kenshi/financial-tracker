import { type FC } from 'react';
import s from './ExpenseCard.module.scss';

interface ExpenseCardProps {
  className?: string;
}

export const ExpenseCard: FC<ExpenseCardProps> = ({ className }) => {
  return <div></div>;
};
